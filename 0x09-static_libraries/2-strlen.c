#include <string.h>

/**
 * _strlen - Takes a string and returns the length of the string
 *
 * @s: The string to get the length of
 *
 * Return: The length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
